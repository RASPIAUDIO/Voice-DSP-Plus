# USB 16 kHz AUTO

Voice-optimized USB firmware with two processed inputs, two outputs and
automatic LINE/SQUARE geometry detection at cold boot.

This is an advanced alternative to the recommended 48 kHz USB firmware. Use
the `_dfu_upgrade.bin` image with the browser updater or a compatible DFU tool.
The `.xe` image is intended for XTAG development.
